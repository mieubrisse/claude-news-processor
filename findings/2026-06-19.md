Claude News — 2026-06-19
========================

Blog Digest
-----------

7 new posts since 2026-06-12.

- **[Steering Claude Code: CLAUDE.md files, skills, hooks, rules, subagents and more](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more)** — Anthropic's canonical taxonomy of the seven instruction methods for Claude Code: CLAUDE.md (root vs subdirectory), `.claude/rules/`, skills, subagents, hooks, output styles, and append-the-system-prompt. Each method gets a When-loaded × Compaction-behavior × Context-cost × When-to-use row. This is the framework Kevin's `/cc-configuration` skill family operationalizes — published as the authoritative external version.

- **[Claude Code now supports artifacts](https://claude.com/blog/artifacts-in-claude-code)** — A session can now publish its work-in-progress as a live, shareable web page (PR walkthroughs, dashboards, release checklists, incident investigations). The page auto-republishes in place as the session continues; version history at the same link; org-only by default, can't be made public.

- **[Claude Design now stays on brand for daily work](https://claude.com/blog/claude-design-stays-on-brand-for-daily-work)** — Claude Design now imports a design system from a GitHub repo, design files, or raw uploads; `/design-sync` pulls a design system from Claude Design into Claude Code; `/design` lets you create, edit, and sync design projects from the Claude Code terminal. Net: Claude Code↔Claude Design becomes a single workflow.

- [Meet the winners of the Built with Opus 4.7 Claude Code hackathon](https://claude.com/blog/meet-the-winners-of-built-with-opus-4-7-claude-code-hackathon) — Case-study post on hackathon winners (medical training, electronics repair, CS ed, factory maintenance, etc.). No transferable technique surfaced; one-line per calibration rule on case-study posts.

- [Meet the winners of our Claude Opus 4.8 Build Day hackathon](https://claude.com/blog/meet-the-winners-of-our-claude-opus-4-8-build-day-hackathon) — Case-study post on a Claude Opus 4.8 day-long hackathon (Tang Dynasty architecture reconstruction, synthetic-SF polling, etc.). No transferable technique surfaced; one-line per calibration rule.

- [Centrally manage authorization for MCP connectors](https://claude.com/blog/enterprise-managed-auth) — Enterprise feature: admins centrally manage MCP authz for Cowork. Skipped per Standing Calibration Rule (enterprise/compliance scope, no solo-founder-transferable technique surfaced).

- [Workload Identity Federation now generally available on the Claude Platform](https://claude.com/blog/workload-identity-federation) — Enterprise feature: short-lived OIDC tokens instead of long-lived service account keys for the Claude Platform. Skipped per Standing Calibration Rule (enterprise/cloud-auth scope, no solo-founder applicability).

Suggested Improvements
----------------------

### Skills / CLAUDE.md

**1. The 7-method taxonomy is the authoritative external version of what `/cc-configuration` already operationalizes — cite it from there.**

- **What:** `~/app/dotfiles/claude/skills/cc-configuration/SKILL.md` (and the umbrella siblings `/cc-global-instructions-management`, `/cc-rules`, `/cc-skill-management`).
- **Why:** The Steering post publishes Anthropic's own decision framework for *which method gets which kind of instruction* (CLAUDE.md vs rules vs skills vs subagents vs hooks vs output styles vs append-system-prompt), keyed by When-loaded × Compaction-behavior × Context-cost. Kevin's existing skill family already enforces this — but it doesn't cite an external source. Citing the post inside `/cc-configuration` gives the agent an authoritative reference for tie-breaking when "should this be a rule or a skill?" arises mid-session. It also turns Kevin's setup into a working example of the post's framework, which is a useful framing when explaining the system to others.
- **How:** Spawn a mission to `mieubrisse/dotfiles` referencing this findings file and the blog URL. The dotfiles agent figures out where the citation lands (inside `/cc-configuration` body, or a "See also" footer in each sibling skill). The exact placement isn't load-bearing.
- **Impact:** Medium.
- **Confidence:** Likely.

**2. Kevin's setup has no `output styles` and no `append-system-prompt` — worth a deliberate look at whether either fits anywhere.**

- **What:** A one-mechanical-question audit, not an edit.
- **Why:** Of the seven methods, Kevin actively uses CLAUDE.md (root + project-level), `.claude/rules/`, skills (heavily), subagents (heavily), and hooks. **Output styles** ("significant role changes — code assistant to general assistant", session-start, never compacted) and **appending the system prompt** ("tone, response length, formatting preferences", CLI flag, never compacted) are the two methods that don't appear anywhere in the dotfiles repo or in Kevin's habitual usage. That's either a real gap or evidence those methods don't fit the solo-founder exobrain use case. Either answer is informative.
  - Output styles candidate: a deliberate "creative writing" or "reflection-mode" persona that overwrites the default for the session.
  - Append-system-prompt candidate: per-mission tone/format directives passed at `agenc mission new` time without bloating CLAUDE.md.
- **How:** Not a mission spawn — this is a question to sit with, possibly the mechanical "how exactly does each method work" follow-up Kevin tends to engage with. The article describes both at enough depth that the next question is *how to apply it*, not *what is it*.
- **Impact:** Medium if either method fits a real use case Kevin currently handles awkwardly; low otherwise.
- **Confidence:** Speculative on adoption, certain on the gap being real.

**3. The article's "Keep CLAUDE.md under 200 lines" tip cuts against Kevin's CLAUDE.md size — but the tip's reasoning is *team-context cost*, not solo-context cost. Worth verifying the divergence is deliberate.**

- **What:** A check, not a recommendation to shrink. The article says: "Keep CLAUDE.md under 200 lines, give it an owner, and review changes to it like code." Reasoning given: "every line loads into every session for every engineer working in the repo, whether it's relevant to their task or not."
- **Why:** Kevin's CLAUDE.md is significantly over 200 lines. But (a) it's solo, not shared — "every engineer in the repo" reduces to "Kevin's agents only"; (b) `/cc-global-instructions-management` already enforces progressive disclosure (routing to skills rather than restating); (c) Kevin's CLAUDE.md is intentionally an *index of the exobrain*, which the article's framing doesn't account for. So the divergence may be correct.
  - The genuine question worth surfacing: even with the index framing, *is there content currently in CLAUDE.md that should live as a skill or rule instead?* The article's per-method table is the checking tool. Specifically: standing rules like "Plan Mode", "Git Command Rules", "Browser", "Markdown" — those look more like `.claude/rules/` content than CLAUDE.md content under Anthropic's taxonomy. Rules are "Always-on unless path-scoped" → very similar to CLAUDE.md but with the option to path-scope.
- **How:** A pass over the current CLAUDE.md against the article's 7-method table, asking "if I were placing this rule into Kevin's setup *today* with the taxonomy in hand, where would it go?" Best done as a mission against `mieubrisse/dotfiles` with this findings file as the brief.
- **Impact:** High *if* a meaningful portion of CLAUDE.md belongs in `.claude/rules/`; medium otherwise.
- **Confidence:** Likely that there's some movable content; speculative on how much.

### AgenC config

**4. Artifacts-in-Claude-Code is mostly team-shaped, but the live-republishing pattern is interesting against Kevin's `/html-output` skill — worth a mechanical comparison, not adoption.**

- **What:** A comparison between Anthropic's artifacts feature and Kevin's existing `/html-output` skill.
- **Why:** The artifacts feature is built for team collaboration (org-only sharing, live updates pulled by teammates, no public mode), which doesn't match Kevin's solo-founder threat model — same calibration call as last week's vault-env-vars finding. But two mechanical aspects of artifacts are interesting against `/html-output`:
  1. **Auto-republish in place** — every Claude Code update to the artifact refreshes the open page, and the URL stays stable. Kevin's `/html-output` produces a one-shot HTML file. A long-running mission that wanted to surface progress could plausibly publish an artifact-style live page that refreshes as work continues. But Kevin's AgenC notification pattern already serves this role from a different angle (push to user vs pull a live page). Probably not a real gap.
  2. **Built from full session context (codebase + connectors + conversation)** — Kevin's `/html-output` is invoked with content the agent has already gathered; artifacts pulls from session context automatically. This is closer to "auto-detect what's worth rendering" vs "render this thing I'm telling you to render." Probably a feature difference that maps to Anthropic's UX vs Kevin's tool-discipline preferences, not a real lever.
- **How:** No mission spawn. This is a "hold the comparison" question — flag if a future use case surfaces that artifacts would serve better than `/html-output` + notifications.
- **Impact:** Low.
- **Confidence:** Speculative.

### Workflows

**5. The Claude Design `/design` + `/design-sync` slash commands inside Claude Code make landing-page iteration potentially much shorter — but require swapping out Kevin's current `/tech-landing-page-creation-pipeline` flow.**

- **What:** A flagged-for-later finding, not an immediate change.
- **Why:** Kevin's landing page work today goes through `/tech-landing-page-creation-pipeline` (six phases, copy-driven) and uses `/website-screenshotting` to verify. The new `/design-sync` command pulls a design system from Claude Design into Claude Code; `/design` lets you create, edit, and sync design projects without leaving the terminal. If Kevin's landing-page iteration could be shortened by having Claude Code drive Claude Design as the visual editor (instead of generating HTML/CSS from scratch through the pipeline), that's a step-change. But:
  - Kevin's current pipeline is opinionated and tightly tuned (per the SKILL.md history); replacing the visual phase with Claude Design loses that specificity.
  - Claude Design is its own tool surface Kevin hasn't been using; learning the canvas + iteration UX has its own cost.
  - The use case for Kevin is solo landing pages, not team design-system enforcement.
- **How:** Flagged-for-later, not an action item. If Kevin's next landing-page project hits friction at the visual-design phase, this is the lever to consider.
- **Impact:** Potentially high if the visual phase is the friction point; low if copy is what bottlenecks.
- **Confidence:** Speculative.

Feedback
--------

How did this digest land?

- **Anything I should drop?** Did I surface a suggestion that's obviously not for you (wrong setup match, generic pattern, enterprise-only)?
- **Anything I should dig deeper into?** Is there a concept that's worth a mechanical "how exactly does this work" follow-up?
- **Anything I missed?** A blog post you'd want surfaced differently, or a setup-area I'm not weighting?
- **Any standing-rule candidates?** A *pattern* (not a one-off) in my suggestions that should be encoded as a Standing Calibration Rule in `CLAUDE.md`?

The next run reads this mission's transcript and updates the Standing Calibration Rules section of `CLAUDE.md` based on your response. No separate tuning step.
