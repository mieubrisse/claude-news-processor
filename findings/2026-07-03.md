2026-07-03 — Claude News Findings
=================================

Blog Digest
-----------

**High-relevance**

- **[Getting started with loops](https://claude.com/blog/getting-started-with-loops)** (2026-06-30). Anthropic publishes its canonical taxonomy for agentic loops: turn-based, goal-based (`/goal`), time-based (`/loop` and `/schedule`), and proactive. Each is defined by trigger, stop criteria, primitive, and best-fit task. Introduces `/goal <criterion>, stop after N tries` as a first-class Claude Code primitive that runs an evaluator model between turns until the criterion is met or the turn cap trips.
- **[Building effective human-agent teams](https://claude.com/blog/building-effective-human-agent-teams)** (2026-06-24). Frames Anthropic's move from single-player AI to multiplayer via Claude Tag. Four lessons distilled from Anthropic's internal experiments: (1) work in public and default context to broadly-accessible so agents can find it; (2) every human and agent has a defined role with distinct tools; (3) agents get their own credentials, not human proxies; (4) transparency reduces per-item sharing decisions.

**Low-relevance (one-line acknowledgment per calibration rule)**

- **[Claude in Microsoft Foundry is now generally available](https://claude.com/blog/claude-in-microsoft-foundry)** (2026-06-29). Enterprise cloud GA on Azure. Skipped.
- **[The full Claude Desktop experience on AWS, Google Cloud, and Microsoft Foundry](https://claude.com/blog/the-full-claude-desktop-experience-on-aws-google-cloud-and-microsoft-foundry)** (2026-06-22). Enterprise Desktop packaging via IAM Identity Center / Entra / Okta. Skipped.
- **[Introducing the Claude apps gateway for Amazon Bedrock and Google Cloud](https://claude.com/blog/introducing-the-claude-apps-gateway)** (2026-06-29). Self-hosted control plane for Claude Code SSO, cost attribution, spend caps on multi-developer enterprise deployments. Skipped.
- **[Giving admins more visibility and control over Claude spend](https://claude.com/blog/giving-admins-more-visibility-and-control-over-claude-usage-and-spend)** (2026-07-02). New Enterprise admin analytics, model-level entitlements, spend alerts. Skipped.
- **[Agent identity in Claude Tag](https://claude.com/blog/agent-identity-access-model)** (2026-06-24). How admins configure per-workspace agent identity in Claude Tag (Slack). Companion piece to the human-agent-teams post but focused on the enterprise identity/access mechanics. Skipped as its own item; the transferable lesson (agents need their own credentials, not human proxies) is captured in the human-agent-teams entry.

Suggested Improvements
----------------------

### 1. Adopt the loop taxonomy vocabulary and name what you already have (medium impact, certain)

- **What.** Add a short "Loop taxonomy" section (or invoke) at the top of the `/loop` and `/schedule` skill bodies that names the four loop types from Anthropic's canonical taxonomy and marks which slot each of your existing loops fits.
- **Why.** You already run every kind of loop in the taxonomy: `/loop` (time-based, in-session), `agenc cron` cron-fired skills like `claude-news-processor`/`hn-daily-pull`/`flight-watcher`/`daily-state-summary`/`arpan-claude-optimization-suggestions` (proactive), plus `verification-before-completion` (goal-shaped, without the primitive). Anthropic just made the vocabulary canonical — grounding your own skills in it makes future skill-authoring decisions ("is this really a loop, and what kind?") faster and gives an external reference for tie-breaking. Also lands well with `/agenc-cron-loops` which already captures the cadence-vs-event distinction.
- **How.** In `/loop` and `/schedule` SKILL.md, add a one-paragraph "This is a time-based loop per Anthropic's taxonomy (link)" pointer plus a sibling-loop cross-ref (`/loop` = same-machine; `/schedule` = cloud). In `/agenc-cron-loops`, add a "This is the proactive-loop primitive" note.
- **Impact / Confidence.** Medium / Certain.

### 2. Evaluate `/goal` as a new Claude Code primitive against your `verification-before-completion` skill (high impact if it lands, likely)

- **What.** `/goal <deterministic criterion>, stop after N tries` is described as a live Claude Code primitive where an evaluator model checks the criterion between turns. Read: this is `verification-before-completion` productized inside the CLI itself.
- **Why.** You already have `verification-before-completion` as a skill and your CLAUDE.md's "Audit completion claims before writing the summary" and "Multi-system work needs evidence per destination" rules operationalize the same idea. `/goal` would let you specify the criterion once at task-start rather than nagging with "how do you know?" mid-summary. The mechanical question worth answering before adopting: does `/goal`'s evaluator-model see your skills / your CLAUDE.md, or is it a naked evaluator that only sees the criterion string? If the latter, wrapping it with a skill that expands "goal: X" into "goal: X per this repo's completion-check protocol" is the right move.
- **How.** Try `/goal` on one venture-repo task where verification-before-completion currently gets invoked (e.g., an IG carousel ship, a landing-page section-copy pass). Compare: does `/goal`'s built-in evaluator catch what your current verification skills catch, or does it rubber-stamp? If it rubber-stamps, the wrapper-skill path is the answer; if it holds standards, a lot of your current review-loop scaffolding could collapse into `/goal <criterion>` calls.
- **Impact / Confidence.** High if the evaluator holds standards / Likely. Ships alignment with an official Anthropic primitive.

### 3. "Write for agents, not just for humans" — audit your exobrain artifacts for agent-findability (medium impact, likely)

- **What.** Anthropic's Lesson 1 from the human-agent-teams post: "for an agent, if it isn't written down and accessible, it doesn't exist." Their concrete practice is defaulting decision-artifacts to be written into channels/docs/meeting-notes so agents can find them.
- **Why.** You already do this heavily: journal entries, venture-repo conversation logs, `/log-venture-repo-conversation`, `/process-meeting`, the `/exobrain` concept. But some categories still fail Anthropic's test — e.g., decisions made verbally in Grain meetings that never make it into a log; opinions held in your head that shape strategy but don't sit in any exobrain file; conversations with your assistant Erika that live in the Work Tracker only. This isn't "write more"; it's "when a future agent would need to know this, is it in a place a future agent could find?"
- **How.** Two candidate structural fixes: (a) a self-refine check that runs after every meeting/conversation flow ("was the decision written down where an agent could find it?"); (b) extend `/process-meeting` and `/log-venture-repo-conversation` with an explicit "decisions surface" that lists any load-bearing decisions made in the meeting, so a future agent grepping for them lands there. Also: your CLAUDE.md's `Provenance Principle` already encodes half of this — worth cross-referencing.
- **Impact / Confidence.** Medium / Likely. This is a refinement of an already-strong pattern, not a new capability.

### 4. Test whether Claude Tag would replace or complement your Erika handoff pattern (speculative)

- **What.** Claude Tag places an agent in a shared workspace (Slack, primarily) with its own credentials. Your current Erika handoff goes through the Kevin/Erika Work Tracker.
- **Why.** The transferable question: if Claude Tag existed in your workspace, would it sit *between* you and Erika (agent triages tasks, escalates to Erika) or *alongside* her (agent takes IG-caption-writing-shaped tasks, Erika takes the human-judgment ones)? Reading the human-agent-teams post, the "defined roles + own credentials" pattern is what makes multiplayer teams work.
- **How.** This is a strategic question, not a mission spawn. Worth noting for the next weekly-plan or a future `/entrepreneurship-advice` session as a "what would leverage look like here?" prompt. Do NOT ship Claude Tag adoption from a cron finding.
- **Impact / Confidence.** Speculative / Speculative. Flagged for your consideration; no action recommended from this run.

Feedback
--------

How did this digest land?

- **Anything I should drop?** Did I surface a suggestion that's obviously not for you (wrong setup match, generic pattern, enterprise-only)?
- **Anything I should dig deeper into?** Is there a concept that's worth a mechanical "how exactly does this work" follow-up?
- **Anything I missed?** A blog post you'd want surfaced differently, or a setup-area I'm not weighting?
- **Any standing-rule candidates?** A *pattern* (not a one-off) in my suggestions that should be encoded as a Standing Calibration Rule in `CLAUDE.md`?

The next run reads this mission's transcript and updates the Standing Calibration Rules section of `CLAUDE.md` based on your response. No separate tuning step.
